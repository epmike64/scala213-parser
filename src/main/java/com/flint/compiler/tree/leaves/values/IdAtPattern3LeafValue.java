package com.flint.compiler.tree.leaves.values;

import com.flint.compiler.token.type.fToken;
import com.flint.compiler.tree.leaves.nodes.ProdRootLeafN;

public class IdAtPattern3LeafValue extends TokenLeafValue {
	public ProdRootLeafN pattern3LeafN;
	public IdAtPattern3LeafValue(fToken kw_token) {
		super(kw_token);
	}
}
