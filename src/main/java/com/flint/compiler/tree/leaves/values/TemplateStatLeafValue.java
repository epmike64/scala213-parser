package com.flint.compiler.tree.leaves.values;

import com.flint.compiler.token.type.fToken;

public class TemplateStatLeafValue extends TokenLeafValue {
	public TemplateStatLeafValue(fToken kw_token) {
		super(kw_token);
	}
}
